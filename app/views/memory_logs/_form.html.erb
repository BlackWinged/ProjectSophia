<%= form_for(memory_log) do |f| %>
  <% if memory_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(memory_log.errors.count, "error") %> prohibited this memory_log from being saved:</h2>

      <ul>
      <% memory_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :timestamp %>
    <%= f.datetime_select :timestamp %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
